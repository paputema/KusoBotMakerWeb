package com.kusobotmaker.repositories;
import org.springframework.data.jpa.repository.JpaRepository;

import com.kusobotmaker.Data.DataFollowRequest;

public interface DataFollowRequestRepositories extends JpaRepository<DataFollowRequest, Long> {
}
